一、程序调用
    参数[0]，程序调用; 参数[1],对比目标图; 参数[2],基准原图;...... 参数[n],程序调用ID
    example: ./compare_picture  /home/zdsb/opencv/192.jpg  /home/zdsb/opencv/191.jpg 123456
二、Http数据上传信息
    1. ID 为程序调用ID
    2. howManyDiff 为不同区域块数
    3. 参数解析
        1> 参数0: 基本相同，无不同区域，
        2> 参数[0,64]: 大致相同，部分区域不同，  
        3> 参数max+1: 提示差距过大，均值Hash算法对比小于60%相似度，
        4> 参数max+3: 提示对比图片像素大小不同
        5> 参数max+4: 程序调用参数错误
        6> 参数max+2: 提示对比图片错位过大

三、版本升级
    1.多基准图
        1> 基准图中SSIM算法选出最相似图，再做相似比较（AI_picture_test4.1）
        2> 基准图中SSIM算法选出最相似图,在选出与最相似差别不大的基准图组(包含最相似图)，基准图组中HASH算法选出最相似图，再做比较（AI_picture_test4.2）
        3> SSIM算法选出基准图组（最相似组和相似度大组），每张基准图比较，相似度100,直接跳出不比较后续基准图，有异常继续比较，直到比较完所有图组或者有相似度100的基准图，取相似度最高图片的信息(AI_picture_test4.3)
        